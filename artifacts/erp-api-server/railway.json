{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"set -e; node --enable-source-maps /app/artifacts/erp-api-server/dist/consolidate-caisses.mjs && node --enable-source-maps /app/artifacts/erp-api-server/dist/index.mjs\"",
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
